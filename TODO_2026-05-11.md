# TODO for 2026-05-11

## Current status

- Migration criticality fixes are in place and passing.
- Runtime safety controls for file writes and code modification are now exposed in the dashboard.
- Targeted validation passed:
  - `ddev exec ./vendor/bin/pest tests/Feature/GenerateMissingFieldE2EValidationTest.php tests/CheckerManagerTest.php tests/LaravelFormsCheckerSchemaTest.php`
- Full `ddev exec ./vendor/bin/pest` still has unrelated failures.

## First tasks tomorrow

1. Fix `tests/ProductionSafetyTest.php`
   - The test still expects `environment('production')` / `if ($this->app->environment` string checks in the service provider.
   - Update the test to reflect the current `isProductionEnvironment()` guard implementation in `ModelSchemaCheckerServiceProvider`.

2. Fix `tests/RelationshipCheckerTest.php`
   - `RelationshipChecker::checkModelRelationshipsForFile()` is typed to `Symfony\Component\Finder\SplFileInfo` but the checker is receiving `SplFileInfo`.
   - Relax the method signature or normalize the file source type so the checker accepts the actual iterator output.

3. Re-run the full suite
   - `ddev exec ./vendor/bin/pest`
   - Confirm there are no new regressions outside the two known failing areas.

## Optional follow-up

- Add inline dashboard warnings near migration/fix actions when runtime safety settings are disabled.
- Consider persisting runtime settings somewhere more explicit than cache if users need them to survive cache clears/deploys.